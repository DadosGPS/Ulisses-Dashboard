import { redirect } from "next/navigation";

/** O histórico de importações e a validação de dados estão na página /sistema. */
export default function ConfigOrigemDadosPage() {
  redirect("/sistema");
}
